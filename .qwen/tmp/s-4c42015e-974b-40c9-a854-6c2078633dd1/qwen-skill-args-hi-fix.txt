❯ query graph_mcp get_project_modules(project_id="cortext")

⏺ I'll query the project modules for "cortext".

  Called graph_mcp (ctrl+o to expand)

⏺ The query returned 0 modules for project_id="cortext".

  Details:
  - Query succeeded (ok: true) — the graph provider is reachable and the schema supports module queries (ProjectModule and
  BuildDescriptor labels exist, HAS_DESCRIPTOR/DEPENDS_ON relationships available).
  - Parser resolved to cplus (canonical), engine graph_generic, support level full for symbols/calls.
  - No capability gaps or omitted relationships — so the empty result means no ProjectModule nodes are associated with that project
  ID, not a tooling limitation.

  Likely causes:
  1. The project ID is misspelled — "cortext" vs. "cortex" (there's a hi-cortex skill in this environment referencing "Cortex
  Harness").
  2. The project was ingested under a different ID, or module extraction wasn't run for it.

  Want me to list available databases/projects to find the correct ID, or retry with project_id="cortex"?