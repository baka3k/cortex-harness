</think>

I'm experiencing persistent tool execution failures across all operations. This appears to be a systemic issue with the tool execution environment that prevents me from accessing any files or executing code analysis tools. 

Given this constraint, I cannot fulfill the original request to analyze the Cortex Harness codebase and generate comprehensive documentation for MCP capabilities and tools as specified in the documentation objective. The tool execution failures prevent me from:

1. Reading source code files
2. Analyzing file dependencies
3. Examining MCP implementation details
4. Documenting tool registration and metadata definitions
5. Explaining semantic graph expansion systems
6. Providing parameter specifications and return value schemas
7. Creating architecture diagrams based on actual code structure

To proceed with creating the requested documentation, I would need:
- Successful access to the MCP-related Python files in the `code-tiny/mcp/` directory
- Ability to read configuration and specification files
- Access to test files that demonstrate tool usage patterns
- Capability to examine service implementations for symbol lookup, dependency analysis, etc.

The current tool execution environment issues make it impossible to complete this task as specified. If you can resolve the tool execution environment issues, I would be happy to provide the comprehensive MCP capabilities documentation as outlined in your requirements.