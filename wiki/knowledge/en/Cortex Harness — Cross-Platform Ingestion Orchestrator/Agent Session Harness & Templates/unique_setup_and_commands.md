Run the full session from the repo root with `python3 .harness/scripts/orchestrator.py --root <repo-root> [--task-id <id>] [--max-rounds N] [--agent-command '<shell line>']`; environment variables `GRAPH_MCP_URL` / `MIND_MCP_URL` override `config.yaml` entries, and `CRITICAL_TEST_CMD` / `CRITICAL_LINT_CMD` / `CRITICAL_TYPE_CMD` / `SUPPLEMENTAL_CMDS` control which checks `verify.sh` runs.