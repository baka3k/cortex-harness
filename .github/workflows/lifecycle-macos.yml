---
name: Lifecycle macOS compatibility

on:
  pull_request:
    paths:
      - ".github/workflows/lifecycle-macos.yml"
      - "Makefile"
      - "pyproject.toml"
      - "cortex_harness/dev.py"
      - "scripts/mcp-lifecycle.py"
      - "scripts/mcp-lifecycle.ps1"
      - "tests/test_dev_lifecycle_commands.py"
      - "tests/test_make_lifecycle.py"
  push:
    branches: [develop]
    paths:
      - ".github/workflows/lifecycle-macos.yml"
      - "Makefile"
      - "pyproject.toml"
      - "cortex_harness/dev.py"
      - "scripts/mcp-lifecycle.py"
      - "scripts/mcp-lifecycle.ps1"
      - "tests/test_dev_lifecycle_commands.py"
      - "tests/test_make_lifecycle.py"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: ${{ matrix.name }} / Python ${{ matrix.python }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Intel
            os: macos-15-intel
            architecture: x86_64
            python: "3.12"
          - name: Apple-Silicon
            os: macos-15
            architecture: arm64
            python: "3.12"

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          version: "0.12.3"
          enable-cache: true
      - name: Install lightweight CLI runtime
        run: |
          uv pip install --system click psutil
          uv pip install --system --no-deps --editable .
      - name: Verify macOS runner tools
        run: |
          test "$(uname -m)" = "${{ matrix.architecture }}"
          test -x /usr/bin/osascript
          command -v make
          command -v dev
      - name: Smoke test global lifecycle help
        run: |
          cd "$(mktemp -d)"
          dev help
      - name: Run lifecycle suite
        run: python -m unittest tests.test_make_lifecycle tests.test_dev_lifecycle_commands -v
