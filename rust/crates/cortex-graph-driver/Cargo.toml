[package]
name = "cortex-graph-driver"
version = "0.1.0"
edition = "2024"
description = "Spike: LadybugDB (lbug crate) driver cho CortexHarness graph core"
license = "MIT"
publish = false

[dependencies]
cortex-graph-core = { path = "../cortex-graph-core" }
lbug = "0.20.4"
serde_json = "1"
sha2 = "0.10"

[lints.rust]
unsafe_code = "deny"
