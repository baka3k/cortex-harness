[package]
name = "cortex-retrieval"
version = "0.1.0"
edition = "2024"
description = "Rust port of the CortexHarness retrieval brain (code-tiny/tools/common)"
license = "MIT"

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]

[lints.rust]
unsafe_code = "deny"
