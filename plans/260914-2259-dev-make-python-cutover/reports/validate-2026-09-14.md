# Validate — critical questions interview (2026-09-14)

4/4 OPEN decision đã chốt bởi user (hướng aggressive, nhất quán với directive cut-maximal):

| # | Câu hỏi | Quyết định | Ảnh hưởng plan |
|---|---|---|---|
| OPEN-1 | ORT provisioning cho `make build` (hiện `ensure_ort.py` cần venv) | **Port sang Rust** — native `ensure-ort` trong cortex-dev: reqwest tải + unzip wheel PyPI pinned | phase-05 thêm work item `ensure-ort`; `make ort-ensure` → binary; `ensure_ort.py` archive ở phase-07; gate "máy sạch" đạt hoàn toàn |
| OPEN-2 | Phase-04 (wire `dev sync` → cortex-sync/cortex-doc) giữ hay tách | **Giữ phase-04** trong plan này | Không đổi — phase-04 là khoản cut lớn nhất (bỏ spawn incremental_sync.py, doc-tiny, journal-consumer recovery, bảng LANG_ANALYZERS .py) |
| OPEN-3 | `dev doctor` python checks | **Bỏ python checks** — Rust doctor chỉ check thứ Rust dùng; output delta chấp nhận | phase-05 action `doctor` đổi spec; parity harness normalise; máy sạch doctor xanh (hợp lệ) |
| OPEN-4 | Rollback python (`CORTEX_DEV_BACKEND=python` + dev.py) giữ bao lâu | **Bỏ ngay phase-06** — binary-only từ lúc flip | D1/D3 rewritten: không có flag rollback; downgrade = previous release binary; pyproject console script xoá hẳn; dev.py parity-reference-only từ phase-06 |

Rủi ro chấp nhận (ghi trong plan.md Risks): binary bug trên máy user sau flip phải hotfix Rust
— mitigate bằng previous-release downgrade path + dogfood gate trước khi archive Python.
