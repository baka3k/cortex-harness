# Red-team rev 1 — 260915-analyzer-layer-rust-cutover

Ngày: 2026-09-15. 4 reviewer độc lập (security / assumptions / failure-modes / scope-complexity) theo `dev-shared/roles/reviewer.md`. 35 findings thô → dedup → 15 findings chốt. Verdict tổng: **FAIL rev 1** — 2 Critical đòi cấu trúc lại phase map.

## Findings chốt (severity, lens nguồn, adjudication)

| # | Sev | Nguồn | Finding | Adjudication |
|---|---|---|---|---|
| 1 | Critical | assumptions (A1) + scope (C1) | Phase-05 citation sai: "cosine 0.9999994/840 vectors" là **bge-m3 doc-plane** (runbook:42-44 → phase02-bgem3-parity.md), không phải jina int8. Jina fp32 = 0.9999995/520 texts; **int8 gate CHƯA chạy** (phase01-jina-parity.md:18), không có code int8 trong cortex-embed. Spike còn có report **NO-GO đo đạc** cho port sync ingest (`spike/reports/phase05-sync-decision.md:8`: uuid5 point-id join order, redaction regex 3 tầng, `_hash_vector` — cosine gates có thể không bắt được). Phase-05 cũng plan-sized (contract chưa định, flip CORTEX_EMBED_BACKEND ngược runbook:44-48) | **ACCEPT** — phase-05 tái cấu trúc: phải formally overturn NO-GO bằng component-parity gates (point-id, redaction, hash_vector) + wall-time baseline + provenance pins (HF revision, sha256 in-repo, ensure-ort fail-closed) + artifact 0600 + mid-history gate trên collection Python-written. Hỏi user hướng xử lý (Q1) |
| 2 | Critical | failure-modes (F1) | Message-scan plane chết im lặng dưới rust children: 17 parsers message-enabled (`registry.rs:295-300`), children accept-and-ignore (`cli.rs:98-99`, analyzer-java main.rs:9), orchestrator vẫn ghi `message_scan_enabled: true` + success khi exit 0 (`orchestrator.rs:1957-2035`). Không phase nào port; phase-06 cho waive; phase-08 xoá script → mất vĩnh viễn | **ACCEPT** — hỏi user (Q2): port native trong plan (khuyến nghị) vs chấp nhận regression có document |
| 3 | High | failure-modes (F3) + security (S1) + assumptions (A10) + scope (C3) | Phase-01 flip de-facto default (unset→Rust-if-binary) TRƯỚC gates 05/06: cửa sổ mất vector/message im lặng cho default path; dogfood đo hệ thống đang di chuyển; flip+delete trước dogfood 7 ngày của runbook; rollback seam bị xoá cùng lúc | **ACCEPT** — phase-01 chỉ wire + opt-in (`=rust`), default giữ Python; flip default + xoá = commit cuối, gated trên composition + dogfood (Q3) |
| 4 | High | failure-modes (F2) + assumptions (A8) | Không implementation nào probe `.exe` (cả registry.rs:284-288 lẫn incremental_sync.py:1412-1413); Windows default ladybug → mọi run delegate; `.venv/bin/python` fallback POSIX-only. Windows chết post-cutover, smoke macOS không thấy | **ACCEPT** — hỏi user (Q4): fix .exe + Windows smoke vs signed out-of-scope |
| 5 | High | assumptions (A2) | Premise phase-06 sai: `realworld_stock_test.py` không có analyzer dual-run (inventory/journal/retrieval only), stock = 116 file .py → không exercise composition 24 parsers/12 overlays | **ACCEPT** — phase-06: synthetic multi-language corpus mới cover đủ 24+12+topology; mỗi port phase (02/03/04) chạy leg orchestrator-parity riêng trước khi merge (C6); stock test bỏ khỏi asset list |
| 6 | High | assumptions (A3) | "29 parity PASS" gồm divergence được nuốt bằng việc thu hẹp fixture (spring dict properties, struts interceptor id-index — umbrella phase-08.md:44-46) → sau xoá, user repo đụng shape đó không có fallback | **ACCEPT** — mỗi quirk thành leg riêng phase-06 hoặc policy post-cutover có document |
| 7 | High | assumptions (A4) | `detector_evidence` divergence (phase09 report:111-115) không nằm trong summary mask (`sync_orchestrator_parity.py:53-61`) → gate "summary khớp" fail spurious hoặc mask ad-hoc giữa gate | **ACCEPT** — phase-06 quyết định trước: fix approximation hoặc thêm mask có lý do |
| 8 | High | assumptions (A6) + scope (C5) + failure-modes (F6) | csharp port xoá capability thật (tree-sitter fallback cho máy không dotnet); rationale port không được ghi (carve-out của digest chưa bị phản biện); Roslyn worker auto-build (`roslyn_adapter.py:92-103`) bị xoá mà không port | **ACCEPT** — phase-03 thêm: gate đo fallback có được dùng thật trên corpus + policy user-facing; port worker bootstrap; ghi rationale vào plan (xoá entry Python là hệ quả trực tiếp của directive "xoá toàn bộ scripts") |
| 9 | High | failure-modes (F5) | Stale-binary: resolution chỉ `is_file()` không version handshake; contract artifact phase-05 tái tạo đúng class bug "rust + qdrant = mất vector" (binary cũ không emit artifact → im lặng) | **ACCEPT** — thêm build-commit handshake (`--version`) + hard-error khi artifact embedding thiếu/invalid |
| 10 | High | failure-modes (F4) | Overlay flip timing tự mâu thuẫn: phase-01 scope wire framework nhưng exit criteria nói "overlays vẫn Python"; không phase nào nói khi nào overlays spawn binary → nguy cơ live spawn trỏ file đã xoá | **ACCEPT** — overlay flip qua map như primary (opt-in từ phase-01, default-flip ở phase cuối); phase-07 thêm leg chứng minh overlay children là binary trước khi xoá |
| 11 | Medium | security (S2,S3,S4) + failure-modes (F10) | Flip matrix không thống nhất 2 implementation: các cell missing-binary / giá trị-khác post-delete undefined; delegated path vẫn silent-fallback vào script đã xoá; contract test phase-14 enshrine divergence; phase-07 grep gate unsatisfiable (rollback maps hợp lệ chứa `_analyzer.py`) | **ACCEPT** — phase-07 mirror đủ matrix (mọi cell) sang delegation target; post-delete: mọi value non-Rust + missing-binary → loud "retired" error cả 2 phía; grep gate đổi thành "references ngoài rollback script_path fields = 0 và maps unreachable khi mode≠python" |
| 12 | Medium | assumptions (A9) | dart: `grammar-versions.toml` không có dart pin; PyPI tree-sitter-dart 0.1.0 vs crates.io có thể lag; plan tham chiếu 260714-1603 đang in_progress (moving target); `--mode all` bị drop khỏi contract | **ACCEPT** — entry criteria phase-02: pin đã verify hoặc vendored grammar; coordinate/pause plan flutter; quyết `--mode all` tường minh |
| 13 | Medium | failure-modes (F7) | `.github/workflows/cobol-macos.yml:47-54` chạy script bị xoá, trigger trên chính delete commit → CI đỏ | **ACCEPT** — phase-07 checklist: update/remove workflows bị ảnh hưởng |
| 14 | Medium | failure-modes (F9) | Rollback = git revert chưa từng test: graph/qdrant ghi trong cửa sổ rust-default không bị revert; không gate chứng minh Python run hội tụ trên mixed-provenance state | **ACCEPT** — phase-08 thêm rollback drill: scratch checkout + revert + re-sync trên copy post-cutover graph/qdrant, require convergence |
| 15 | Medium | scope (C8) + scope (C9) | blocks 2 chiều với spike plan chỉ là claim (spike frontmatter chưa update); `importorskip` làm coverage bốc hơi im lặng; `sync_processes.py`/`run_migration.py`/skills doc không có trong disposition của phase-08 | **ACCEPT** — update spike + umbrella + flutter frontmatter trong phase áp dụng; test skip phải nổi l loud trong gate; audit list nêu đích danh các file |

## Rejected / folded

- Không finding nào bị reject wholesale. Các findings nhỏ (S5 provenance, S6 0600, F8 mid-history, A5 wall-time, A7 qdrant layout/message-vector, C2 dart-dep, C4 mâu thuẫn matrix, C6 incremental composition, C7 terminal state) được **fold** vào fix của findings 1, 3, 5, 10, 11 tương ứng.

## User decisions cần thiết (interview 2026-09-15)

Q1 embedding path, Q2 message-scan, Q3 delete timing, Q4 Windows — xem `## Validation Log` trong plan.md.
