# Phase 03 — Docs

## Tasks

- [ ] Update `ReadMe.md` phần infra-up: mô tả ensure Docker Qdrant + FalkorDB (UI), env overrides (`QDRANT_IMAGE`, `FALKORDB_IMAGE`, ports), ports table (6333/6334/6379/3000).
- [ ] Cross-link plan 260818 (remote support) trong docs nếu cần.
- [ ] Viết `docs/logs/2026-08-20-infra-up-docker-idempotent.md` + prepend entry vào `docs/logs/README.md`.
- [ ] Đánh dấu plan này `status: completed`, cập nhật `relatedPlans` hai chiều với `260818-infra-up-remote-support`.
