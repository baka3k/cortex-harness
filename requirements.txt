qdrant-client==1.18.0
ladybug>=0.20.4,<0.21
falkordblite==0.10.0 ; sys_platform != 'win32'
falkordb ; sys_platform == 'win32'
python-dotenv
pydantic
openai
langextract
google-generativeai
pypdf
python-docx
python-pptx
openpyxl
sentence-transformers
spacy
fastmcp
lxml
gliner
pandas
portalocker>=3.2,<4
psutil>=5.9,<8
# BM25 keyword signal for hybrid retrieval (pin = golden-fixture parity, see
# scripts/rust_parity/gen_bm25_fixtures.py).
rank-bm25==0.2.2
# FastMCP dependencies
# New C/C++ analysis tools
networkx
# Pinned Clang backend for the isolated semantic callsite worker (Phase 02).
libclang==18.1.1
# Additional dependencies for C++ analysis and web services
fastapi
uvicorn[standard]
python-multipart
requests
torch
transformers>=4.41,<4.56
# ONNX graph tooling for the Rust `ort` embedding spike
# (plans/260914-1706-onnx-embedding-spike): int8 quantization via
# onnxruntime.quantization + artifact inspection. fp32 graphs come from the
# official HF `onnx/` subtree, not from a local export.
onnx>=1.17
# torch 2.13 routes torch.onnx.export through the dynamo exporter, which imports
# onnxscript unconditionally; the legacy TorchScript exporter crashes on
# jina-embeddings-v3 (_jit_pass_peephole). See scripts/rust_parity/export_jina_onnx.py.
onnxscript>=0.7
# Existing dependencies
tree-sitter
tree-sitter-rust
tree-sitter-go
tree-sitter-languages
tree-sitter-kotlin
tree-sitter-java
tree-sitter-c
tree-sitter-cpp
tree-sitter-typescript
tree-sitter-javascript
tree-sitter-php
tree-sitter-python
tree-sitter-sql
tree-sitter-c-sharp
tree-sitter-swift
tree-sitter-language-pack

# Deep learning librariespip
einops
